spring:
  datasource:
    driver-class-name: org.h2.Driver
    username: root
    password: root
    url: jdbc:h2:file:D:\Project\bigdata-management-platform\bmp;AUTO_SERVER=TRUE;MODE=MySQL;INIT=runscript from 'classpath:schema_h2.sql'

logging:
  level:
    com.bmp.dao.mapper: debug

platform:
  mem:
    consumer-size: 2
